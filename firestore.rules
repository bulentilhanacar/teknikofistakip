
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow public read access to all documents for now
    // This is not secure for production apps.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
